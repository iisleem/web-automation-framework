[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
    ".venv",
    ".tools",
    "reports",
    "screenshots",
    "videos",
    "traces",
    "downloads",
    "uploads",
    "data/auth",
]

[tool.ruff.lint]
select = ["E9", "F"]
