# Optional email/OTP helper credentials.
# Use a dedicated test mailbox or provider app password. Never commit real secrets.
TEST_EMAIL_USERNAME=qa.automation@example.com
TEST_EMAIL_PASSWORD=replace-with-test-mailbox-app-password

# Optional tokens used by helper examples or project-specific API setup.
# Leave empty unless your tests require them.
API_TOKEN=
